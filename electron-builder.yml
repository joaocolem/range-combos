appId: com.rangecombos.app
productName: Range Combos
directories:
  output: dist
  buildResources: build
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintrc,.prettierrc,tsconfig.json,tsconfig.*.json}'
  - '!{.env,.env.*,.github}'
asarUnpack:
  - resources/**

# Auto-update via GitHub Releases (repo publico joaocolem/range-combos).
# releaseType: release publica direto (nao como rascunho), para o auto-update enxergar.
publish:
  provider: github
  owner: joaocolem
  repo: range-combos
  releaseType: release

win:
  target:
    - nsis

nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  perMachine: false

# Para assinar o Windows depois: defina CSC_LINK e CSC_KEY_PASSWORD no CI.
